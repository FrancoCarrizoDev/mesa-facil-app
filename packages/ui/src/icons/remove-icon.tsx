import { CrossCircledIcon } from "@radix-ui/react-icons";
import type { IIconProps } from "./icons.model";

export default function RemoveIcon(props: IIconProps): JSX.Element {
  return <CrossCircledIcon {...props} />;
}
