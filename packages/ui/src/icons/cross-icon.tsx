import { Cross2Icon } from "@radix-ui/react-icons";
import type { IIconProps } from "./icons.model";

export default function CustomCrossIcon(props: IIconProps): JSX.Element {
  return <Cross2Icon {...props} />;
}
